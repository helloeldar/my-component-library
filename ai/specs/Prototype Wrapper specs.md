Idea:
It would be nice to have prototype wrapper UI — elememnts around prototype that helps to choose different options of prototypes, etc.

Requirements:
UI should differ from out UI kit, so it's easy to distinguishe between real prototype and UI over it. Maybe it's even better to use Shadcn UI for wrapper UI.
It should be similar to Figma prototypes UI: Ability to swtich between options on the left sidebar, ability to hide wrapper UI.
Additinally, it should be able to add different controls, like before / after, checkboxes, dropdowns, etc.
Every prototype ideally should have description what it does.

Also, develop this Interactive steps component, I have screenshot of it here:
/ai/specs/Prototype Wrapper — Intercative steps component.png