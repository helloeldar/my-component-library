# Task - Remove Run and Debug from Left Stripe

## Goal
Remove the "Run" and "Debug" items from the left bottom stripe in the main menu / MainWindow component.

## Requirements
- Remove `run` and `debug` entries from `DEFAULT_LEFT_STRIPE_ITEMS` in `MainWindow.jsx`

## Plan
1. Remove the two entries from the constant array

## Progress
- 2026-03-26: Task created, implementing now

## Results
- Removed `run` and `debug` from `DEFAULT_LEFT_STRIPE_ITEMS`
