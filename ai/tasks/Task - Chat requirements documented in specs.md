# Task — Chat requirements documented in specs

## User messages (original)

1. *“show Tooltips in UI showcase as it is, so i don't have to hover”*
2. *“@AI RULES.md don't forget to write my messages into respective spec as requirements”*

## Interpretation

- **(1)** Tooltip showcase must use the real `Tooltip` component visibly without requiring hover (`alwaysVisible` in code — see prior implementation).
- **(2)** Ongoing process: per **AI RULES.md**, capture user chat requirements in the right **`ai/specs/*.md`** files (structured, preserve meaning; resolve contradictions with the user).

## Plan

- [x] Record **R-Tooltip-Showcase-01** + props/API note in **`ai/specs/Tooltip specs.md`**
- [x] Record **R-Showcase-Specs-01…04** and **R-Showcase-Tooltip-01** in **`ai/specs/UI Showcase (App.js) Specs.md.md`**; align route list with `App.js` (no `/gotittooltip` route today)
- [x] Add this task file for traceability

## Results

- Specs updated 2025-03-23 with structured requirement IDs and verbatim user quotes where relevant.
